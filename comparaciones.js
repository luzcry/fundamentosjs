// si  queremos comparar dos variables se usa == lleva los dos valores a un mismo tipo de dato

var x = 4, y = '4'

x == y

x === y

//siempre que podamos usemos el === 

var luz = {
    nombre: "luz"
}
// si le asignamos luz a esta otra persona va a apuntar a la misma referencia de variable 
var otraPersona = {
    nombre: "luz"
}